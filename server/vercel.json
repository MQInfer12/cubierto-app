{
  "version": 2,
  "builds": [{
    "src": "src/index.ts",
    "use": "@vercel/node"
  }],
  "routes": [{
    "src": "/(.*)",
    "dest": "src/index.ts"
  }],
  "build": {
    "env": {
      "CI": "false"
    },
    "command": "tsc && mv src/index.ts dist/index.ts"
  }
}